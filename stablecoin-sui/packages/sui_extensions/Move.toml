[package]
name = "sui_extensions"
edition = "2024"
license = "Apache 2.0"

# New (sui >= 1.63) package management format. The Sui framework and MoveStdlib
# are implicit system dependencies, auto-resolved per build environment.
# devnet is ephemeral, so build with `--build-env testnet` (identical system
# deps) and publish with `sui client test-publish`. The package's own named
# address comes from `name` above — no [addresses] section in the new system.
[dependencies]
